# ---- Database (Step 1) ----
# Local dev: create DB `isms_lite_dev` on your local Postgres.
DATABASE_URL="postgres://postgres:PASSWORD@localhost:5432/isms_lite_dev"

# ---- Auth.js / Google OAuth (Step 2) ----
AUTH_SECRET="run: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""
AUTH_URL="http://localhost:3000"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
ALLOWED_EMAIL_DOMAIN="maholan.co.th"

# ---- Google Drive (Step 3) — OAuth storage identity ----
# org policy บล็อก Service Account key → ใช้ OAuth token ของบัญชี storage แทน
# บัญชีนี้ต้อง login เข้าแอปครั้งหนึ่ง + เป็น Content Manager ของ Shared Drive
STORAGE_ACCOUNT_EMAIL="surachot.vi@maholan.co.th"
ISMS_SHARED_DRIVE_ID=""

# ---- Email / Resend (Step 5) ----
# RESEND_API_KEY=""
# EMAIL_FROM="ISMS-Lite <no-reply@maholan.co.th>"
